[ ] Refactor: Replace `TagFunctionState = "FO"` with `TagFunctionState = "FS"` (make sure html client also got fixed)
[ ] Make status bar a window container instead of a string.  It contain a list of 1-row windows: usage window, queue window, etc.
[ ] Show running state in status bar (on the left-most) as a green dot/cicle on active, and dimmed one when no prompt is executing.
